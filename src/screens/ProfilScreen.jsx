import * as React from 'react';
import { Text, View } from 'react-native';

import { getCredentials, regenerateToken } from '../security/Credential';

const ProfilScreen = ({ navigation }) => {
  return (
    <div>ProfilScreen</div>
  )
}

export default ProfilScreen